apiVersion: v1
kind: Service

metadata:
  name: agent-svc
  namespace: prod
  labels:
    app: agent

spec:
  type: ClusterIP

  selector:
    app: agent

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
